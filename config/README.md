# Config Conventions

Tracked templates live here. Local machine-specific configs live in repository root `.local/` and are git-ignored.

## Files

- `boar-loot.build.example.json` - template for boar mod build config.

## Local usage

Copy template to:

- `.local/boar-loot.build.json`

and adjust paths/settings as needed.
